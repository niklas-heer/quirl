[project]
name = "quirl-retrieval-model-training"
version = "0.1.0"
description = "Bounded leakage-safe tuning for Quirl's local command retriever"
requires-python = "==3.12.*"
dependencies = [
    "model2vec==0.8.2",
    "torch==2.8.0",
]

[tool.uv]
exclude-newer = "2026-08-18T00:00:00Z"
