// The unpublished release harness deliberately embeds the exact same source
// identity recipe as the CLI so it can prove both artifacts were built from
// one clean candidate.
include!("../quirl-cli/build.rs");
