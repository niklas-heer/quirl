[package]
name = "quirl-plugin"
description = "Versioned plugin manifests, permission locks, and isolated runtime boundaries for Quirl"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
quirl-catalog = { path = "../quirl-catalog" }
quirl-contract = { path = "../quirl-contract" }
quirl-core = { path = "../quirl-core" }
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
toml.workspace = true
wasmparser.workspace = true

[dev-dependencies]
wat = { version = "1.256", features = ["component-model"] }
wit-parser = { version = "0.256", default-features = false, features = ["std"] }
